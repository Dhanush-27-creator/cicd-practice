name: My First CI Pipeline

on:
  push:
    branches: [ env-secrets ]    # ← matches your current branch

jobs:
  run-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Say Hello
        run: echo "Hello from GitHub Actions!"
